<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ExamTimeTable</title>
    <style>
      h2, h3{
        margin-block: auto;
      }

      .heading {
        display: grid;
        align-items: center;
        justify-content: center;
        word-spacing: normal;
      }

      .table {
        display: grid;
        margin-top: 10px;
        justify-content: center;
      }

      table {
        border-collapse: collapse;
        margin: 20px auto;
      }

      th, td {
        padding: 10px;
        text-align: center;
        border: 1px solid #000;
      }
    </style>
  </head>
  <body>
    <h1> Works </h1>
    <div class="heading">
      <h2><%= Doorkeeper::Application.first.name %></h2>
      <h3><%= examination_name %> <%= academic_year %></h3>
      <h3><%= examination_type %> <%= examination_time %></h3>
    </div>
    <div class="table">
      <table>
        <% binding.pry %>
        <thead>
          <tr>
            <th> Sr. No </th>
            <th> Subject Name </th>
            <th> Subject Code </th>
            <th> Day and Date </th>
            <th> Time </th>
          </tr>
        </thead>
        <tbody>
          <% exam_time_tables.each_with_index do |exam_time_table, index| %>
            <tr>
              <td> <%= index + 1 %> </td>
              <td> <%= exam_time_table.subject_name %> </td>
              <td> <%= exam_time_table.subject_code %> </td>
              <td> <%= exam_time_table.examination_date + ' ' + exam_time_table.day.upcase %> </td>
              <td> <%= exam_time_table.examination_time %> </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div style="position: absolute; bottom: 0; display: grid; justify-content: center;">
      <p>By Exam Coordinator : _____________________ </p>
    </div>
  </body>
</html>